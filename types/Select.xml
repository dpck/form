<types>
  <type name="SelectProps" desc="Options for the Select component." noToc>
    <prop opt boolean name="required">
      Whether this is a required field.
    </prop>
    <prop opt string name="name">
      The select name.
    </prop>
    <prop opt string name="value">
      The initial value.
    </prop>
    <prop opt type="Array<{value: *, title: string}>" name="options">
      The array with options to render inside of the `select` element.
    </prop>
  </type>
</types>