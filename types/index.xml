<types>
  <type name="FormProps" desc="Options for the Form component." noToc>
    <prop opt type="function" name="onChange">
      The callback to call when a change is made to any of the inputs inside of the form.
    </prop>
    <prop opt type="function" name="formRef">
      The function to call with the reference to the form HTML.
    </prop>
    <prop opt type="function" name="onSubmit">
      The function to call on form submit.
    </prop>
  </type>
  <type name="FormGroupProps" desc="Options for the FormGroup component." noToc>
    <prop opt string name="label">
      The label to display for the group.
    </prop>
    <prop opt string name="help">
      The help text to show in `<small className="form-text text-muted">{help}</small>`
    </prop>
  </type>
  <type name="SubmitButtonProps" desc="Options for the SubmitButton component." noToc>
    <prop boolean name="loading" default="false">
      Whether the button should display as loading.
    </prop>
    <prop opt string name="loadingText">
      The text to show during the loading progress.
    </prop>
    <prop string name="confirmText">
      The text for the normal state.
    </prop>
    <prop opt string name="className">
      The class name, such as `btn-lg`.
    </prop>
    <prop type="('primary'|'secondary'|'success'|'danger'|'warning'|'info'|'light'|'dark')" default="primary" name="type">
      The type of the button to add to the class as `btn-{type}`.
    </prop>
    <prop boolean default="false" name="outline">
      Display the outline style of the button via setting the `btn-outline-{type}` class.
    </prop>
  </type>
</types>