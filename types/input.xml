<types>
  <type name="InputProps" desc="Options for the Input component." noToc>
    <prop opt boolean name="required">
      Whether this is a required field.
    </prop>
    <prop opt string name="name">
      The input name.
    </prop>
    <prop opt string name="placeholder">
      The input placeholder.
    </prop>
    <prop opt string name="value">
      The initial value.
    </prop>
    <prop opt string name="type">
      The input type.
    </prop>
    <prop opt name="...props">
      All other options to be passed to the input element. When compiling with _Depack_, the props must be added like `<Input {...({ 'onClick': test })}>`
    </prop>
  </type>
</types>