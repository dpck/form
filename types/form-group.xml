<types ns="_depackForm">
  <type name="FormGroupProps" desc="Options for the FormGroup component." noToc>
    <prop opt string name="label">
      The label to display for the group.
    </prop>
    <prop opt string name="className">
      The additional class name to add to `form-group`.
    </prop>
    <prop opt string name="labelClassName">
      The additional class name to add to the label.
    </prop>
    <prop opt string name="col">
      If any of the `col` properties are passed (e.g., `col-12`, `col-sm-8`, _etc_), they will be set on the label.
    </prop>
    <prop opt boolean name="row">
      Whether the group should be displayed in a row. Children must manually be wrapped in `div`s with `col` classes. Adds the `col-form-label` class to the label and the `row` class to the group.
    </prop>
    <prop opt boolean name="form-row">
      Same as `row`, but adds the `form-row` class to the group.
    </prop>
    <prop opt boolean name="details">
      Whether to display the group in `details` block.
    </prop>
    <prop opt string name="help">
      The help text to show in `＜small className="form-text text-muted"＞{help}＜/small＞`. To support validation with `valid` and `invalid` classes, set help on inputs rather than group.
    </prop>
  </type>
</types>